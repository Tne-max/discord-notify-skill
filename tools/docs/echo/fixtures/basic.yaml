name: echo-basic
lane: deterministic
target:
  kind: tool
  ref: docs.echo
inputs:
  message: hello
expect:
  status: sealed
  output:
    subset:
      schema: discord.notify.echo.v1
      data:
        message: hello
